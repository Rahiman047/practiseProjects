import React from "react";

const ShowJsx = (userData) => {
  console.log(userData);
  // const { description, company, category } = eachData;

  return (
    <>
      {/* <h1>{e.company}</h1>
      <h2>{description}</h2>
      <p>{category}</p> */}
    </>
  );
};

export default ShowJsx;
